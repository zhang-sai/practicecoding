package g;

/**
 * remove duplicate lines of a file(what if the file is very large which could 
not be held in the main memory)
 * */
public class RemoveLineDuplication {

	/**
	 * 1. do external sort, and check it
	 * 
	 * 2. hash each line to different memory location, and then sort
	 * 
	 * 3. then do hash code
	 * 
	 * 4. approximately, use bloom filters or count-min
	 * */
	//1. 
	//external sort
	
	//hash and then finger print
	
}
