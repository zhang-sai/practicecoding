/**
 * given an array, find out the max sum of a set that each elements are non-
: ajacent
 * */

public class NonAdjacentSumInAnArray {

	//a typical db
	//maxsum[i] ==> the largest sum of a set so far
	//maxsum[0] = a[0]
	//maxsum[i] =
	//       1. take i, value[i] + maxsum[i-2]
	//       2. not take i, take maxsum[i-1]
	//a O(n)
	
	//see MaxNonContagiousSubArraySum
	
}
